# ─────────────────────────────────────────────
# 硬性规则：命中即禁止，必须修复
# ─────────────────────────────────────────────

# DISTINCT / COUNT(DISTINCT) → 消除 DISTINCT 写法
- SQL-DISTINCT-001

# COUNT(*) → 消除 COUNT(*) 写法
- SQL-COUNT-001

# COUNT(DISTINCT ...) → 消除 COUNT(DISTINCT) 写法
- SQL-COUNT-DISTINCT-001

# RIGHT JOIN → 消除 RIGHT JOIN
- SQL-JOIN-RIGHT-001

# FROM a, b WHERE → 改为显式 JOIN ... ON
- SQL-JOIN-IMPLICIT-001

# SELECT * → 改为显式字段
- SQL-SELECT-STAR-001

# ─────────────────────────────────────────────
# 规范检查：必须符合约定
# ─────────────────────────────────────────────

# 多表/JOIN/子查询时字段必须带表别名
- SQL-TABLE-ALIAS-001

# 子查询必须有别名
- SQL-SUBQUERY-ALIAS-001

# 聚合/计算字段必须有清晰别名
- SQL-FIELD-ALIAS-001

# 临时表命名规范
- SQL-TEMP-TABLE-001

# 必须有分区过滤
- SQL-PARTITION-001

# JOIN 前必须先过滤
- SQL-JOIN-FILTER-001

# GROUP BY 字段完整性
- SQL-GROUP-BY-001

# 除法必须有 NULL 和除零保护
- SQL-NULL-ZERO-001
